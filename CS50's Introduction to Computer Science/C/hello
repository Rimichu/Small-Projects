#include <stdio.h>
#include <cs50.h>

int main(void)
{
    // asks the user for their name
    string name = get_string("What is your name: ");
    // prints "hello, name"
    printf("hello, %s\n", name);
}